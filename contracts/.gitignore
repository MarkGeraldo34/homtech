# Foundry build artifacts and dependencies (reinstalled via forge install / forge build)
out/
cache/
lib/
broadcast/

# Secrets
.env

# Slither venv (pip install slither-analyzer into contracts/.venv)
.venv/
